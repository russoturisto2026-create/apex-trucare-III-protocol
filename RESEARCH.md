# Протокол исследования

Хронологический журнал изучения **протокола помпы TruCare III** — команд, которые помпа принимает,
и ответов, которые она отдаёт. Приложение **Erwin Blueberry** используется только как штатное
средство управления помпой и само не изучается. Одна запись — один шаг (и один коммит). Каждый шаг
ссылается на исходные материалы по номерам из `MATERIALS.md`; выводы шага переносятся в
`PROTOCOL.md` со ссылкой на этот шаг.

Правила ведения — в `README.md`. Ключевое: вывод получает статус **«подтверждено»** только после
≥2 измерений с различными характеристиками (правило 8); до этого — **«предположение»**.

Легенда статусов: 🟡 предположение · 🟢 подтверждено · 🔴 требует перепроверки.

**Граница исследования:** подсчёты и захваты ведутся **с 16:34 (16.09.2026)** — более ранняя
активность стенда (в т.ч. операции пользователя до этого рубежа) является пред-исследовательским
контекстом, задавшим начальное состояние помпы, и в выводы не включается. Захваты, начатые до
рубежа (напр. первичное подключение в Шаге 001), используются только в части, попадающей в окно, а
их выводы принимаются лишь при подтверждении в окне (напр. топология/авторизация — Шаг 003, 16:52).

---

## Этап A — Транспорт и топология GATT

- Шаг 001 — топология GATT, MTU, включение уведомлений. Одно измерение.
- Шаг 003 — второе измерение (переподключение): топология совпала → **🟢 подтверждено**.

## Этап B — Установление сессии и авторизация

- Шаг 003 — авторизация по Bluetooth-паролю (канал 0xFFC0): запись пароля → вердикт 0x00. Статус 🟢.

## Этап C — Формат кадра

- Шаг 004 — гипотезы формата кадра (команда `0x35…`, ответ `0xAA…`, CRC-16/MODBUS). Статус 🟡.

## Этап D — Команды чтения и объекты ответа

- Шаг 004 — гипотезы каталога объектов (`a3/00,08,0c,07,21,26,…`). Статус 🟡.
- Шаг 006 (G1) — статус: метка времени и напряжение батареи 🟢; текущий базал (шаг 0,025) 🟡.
- Шаг 009/010 — макс. болюс off26 и макс. базал off24 подтверждены сменой в приложении 🟢.
- Шаг 007 (G1, анализ, окно ≥16:34) — резервуар (u32, 0,001 Ед) 🟢; счётчик подачи (0,025 Ед) и базал 🟡.
- Шаг 008 — карта разбора объектов `OBJECTS.md`; кандидаты в `a3/00`: макс. базал off24, макс. болюс off26 🟡.

## Этап E — Управляющие команды

- Шаг 004 — гипотезы: `a1/31` установка времени, квитанция `a1/55`. Статус 🟡.
- Шаг 009 — `a1/31` установка времени 🟢; `a1/32` запись настроек (макс. болюс подтверждён сменой 12,0→12,3) 🟢.
- Шаг 011 — серия настроек: `a1/32` (скорость болюса, расш. болюс), `a1/34` (звук. режим, шаг звука) 🟢.
- Шаг 012 — `a1/32` байт[6] битовое поле (+напоминание о ГК); `a1/11` предустановки болюса 🟢.
- Шаг 013 — доп. настройки (`a1/32`): авто-выкл [3], «инсулин заканч.» [4]/[5], таймаут экрана [8..9], лимит дозы [10..11] 🟢; флаги/энумы [0]/[1]/[2]/[7] 🟡.
- Шаг 014 — скорость болюса Низкая→Нормальная: `a1/32` байт[0] bit0 1→0 (оба направления) и `a3/00` off2 🟢.
- Шаг 015 — расш. болюс и напоминание о ГК ВЫКЛ: `a1/32` байт[6] `03→02→00` (оба бита в обе стороны) и `a3/00` off4 (0x02/0x04) 🟢.
- Шаг 016 — звуковой режим ВЫКЛ: `a1/34` байт[0] 1→0 (оба направления); отражение в `a3/0c` off4 (режим) и off5 (шаг звука) 🟢.
- Шаг 017 — вид сигнала тревоги: `a1/32` байт[1] и `a3/00` off1 = 0 Звуковой / 1 Вибрация / 2 Звуковой и вибрация 🟢.
- Шаг 018 — длительность сигнала тревоги: `a1/32` байт[7] и `a3/0c` off2 = 0 Длинный / 1 Нормальный / 2 Короткий 🟢.
- Шаг 019 — яркость экрана помпы: `a1/32` байт[2] и `a3/00` off3 = индекс меню (10/30/50/60/80/100 %) 🟢.

## Этап F — Поведение и тайминги

- Шаг 005 — «пульс» помпы `a5/03`: период 180 с, постоянная нагрузка. Статус 🟡 (один захват).

---

## Журнал шагов

### Шаг 000 — Каркас проекта, методология, фиксация лицензионного соглашения
- **Дата:** 2026-09-16.
- **Что сделано:** создан каркас репозитория (README с правилами проекта, скелеты `RESEARCH.md`,
  `PROTOCOL.md`, `MATERIALS.md`), навык подключения к стенду `apex-device`, зафиксирована версия
  лицензионного соглашения приложения (`user_agreement.md`).
- **Материалы:** нет (организационный шаг).
- **Выводы протокола:** нет.

### Шаг 001 — Подключение помпы: топология GATT, MTU, включение уведомлений
- **Дата/время:** 2026-09-16, подключение ~16:22 (захват 16:21–16:25).
- **Исследование:** `etap-A-transport-gatt`.
- **Действие:** пользователь штатно подключил помпу через приложение. Способ: в приложении введён
  **серийный номер помпы** (значение не приводится), после чего помпа появилась в списке выбора;
  тап по строке помпы с этим серийным номером инициировал подключение. Далее — экран «Устройства»
  со статусом помпы. Приложение велось вручную; собственные кадры помпе не отправлялись (только
  наблюдение).
- **Материалы:** №1 (HCI-захват; из идентификаторов наблюдался только адрес помпы),
  №2 (скриншот «Устройства», серийный номер замаскирован).
- **Разбор:** `tools/hci_topology.py` по материалу №1.
- **Наблюдения:**
  - GATT-сервер помпы: службы `0x1800`/`0x1801`/`0x180A` и вендорские `0xFFE0`, `0xFFE5`,
    `0xFF90`, `0xFFC0` (диапазоны handle — PROTOCOL.md §1.1).
  - MTU: запрос `517` → выдано `251`.
  - Канал ответов помпы: `0xFFE4` (`0x001C`, NOTIFY) — за сессию 40 уведомлений; включён
    записью `01 00` в CCCD `0x001D`.
  - Канал команд: `0xFFE9` (`0x0021`, WRITE/WRITE_NR).
  - Канал `0xFFC0`: `0xFFC1` (WRITE) + `0xFFC2` (NOTIFY, CCCD `0x0045` включён `01 00`,
    1 уведомление) — предположительно авторизация; детально в Этапе B.
  - Сверка радио↔экран: экран показал прошивку `1.1.1`, версию протокола `4.12`, батарею
    `1,36 В`/`46%`, статус «Разблокирована». Сопоставление этих значений с конкретными кадрами
    ответов — задача Этапа D.
- **Вывод:** топология транспортного уровня зафиксирована в PROTOCOL.md §1 со статусом
  🟡 предположение (одно измерение). Для 🟢 требуется повторный захват с иными характеристиками
  (переподключение / после перезагрузки телефона) — топология должна совпасть.

### Шаг 002 — Провенанс лицензионного соглашения (скриншоты)
- **Дата/время:** 2026-09-16, 16:15–16:17 (снято пользователем).
- **Исследование:** `provenance-user-agreement`.
- **Действие:** сохранён комплект скриншотов экрана лицензионного соглашения приложения
  Erwin Blueberry (пролистан целиком). Организационный/провенанс-шаг, обмена с помпой нет.
- **Материал:** №3 — 33 кадра (Части 1–21), покрывают текст `user_agreement.md`. ПДн на кадрах
  не выявлено, маскирование не требовалось. В архив вложен манифест `SHA256SUMS.txt` с SHA-256
  каждого кадра; в реестре зафиксирован SHA-256 самого манифеста как якорь целостности.
- **Проверка соответствия:** первый кадр — заголовок и начало Части 1; последний — Часть 21
  (форс-мажор); совпадает с текстом `user_agreement.md`.
- **Вывод:** зафиксированная в `user_agreement.md` версия соглашения (приложение 1.0 Build 489
  Patch1) подтверждена скриншотами из экземпляра приложения (материал №3).

### Шаг 003 — Переподключение: подтверждение топологии (Этап A) и авторизация по Bluetooth-паролю (Этап B)
- **Дата/время:** 2026-09-16; отключение 16:51:21, переподключение 16:52:02.
- **Исследование:** `etap-B-avtorizaciya`.
- **Действие:** пользователь штатной функцией приложения отключил помпу («Вспомогат. функции» →
  «Опасная зона» → «Отключить помпу»), затем подключил заново. При подключении, помимо серийного
  номера, приложение запросило **пароль помпы** (Bluetooth-пароль), берётся из меню помпы
  «СОСТОЯНИЕ», строка `B/P:` сразу после `S/N:`. Значения серийника и пароля не приводятся.
  Приложение велось вручную; собственные кадры помпе не отправлялись.
- **Материалы:** №4 (HCI, обе сессии), №5 (скриншоты переподключения; PIN и серийник
  замаскированы), №6 (фото экрана «СОСТОЯНИЕ» помпы; S/N и B/P замаскированы).
- **Разбор:** `tools/hci_topology.py` и `tools/hci_auth.py` по материалу №4.
- **Этап A (подтверждение топологии):** сессия переподключения (conn 0x0005, 16:52) выполнила
  полное обнаружение GATT; службы, характеристики, handle, MTU (517→251) и порядок включения CCCD
  побайтово совпали с Шагом 001 (conn 0x0003). Два измерения с различными характеристиками
  (разные сессии/время, после цикла штатного отключения) → топология в PROTOCOL.md §1 переведена
  в 🟢.
- **Этап B (авторизация):** последовательность на подключении: (1) `01 00` в CCCD 0x0045
  (вердикт-канал 0xFFC2); (2) запись пароля в 0xFFC1 (0x0041) — 12 байт; (3) вердикт на 0xFFC2
  (0x0044) — 1 байт, `0x00` (успех); (4) MTU 517→251; (5) `01 00` в CCCD 0x001D (канал ответов
  0xFFE4); (6) команды. Кодирование пароля: 12 байт — шесть ASCII-цифр строки `B/P`, записанных
  дважды подряд (значение не приводится). Сверка радио↔экран: ASCII-цифры на радио совпадают со
  строкой `B/P` на экране помпы (материал №6) — независимое подтверждение. Наблюдено в обеих
  сессиях → PROTOCOL.md §3 со статусом 🟢.
- **Прочее (для будущих этапов):** в захвате также видны кадры команд (`0xFFE9`, начинаются с
  `0x35`, содержат идентификационный блок `APEX<serial>`) и ответов (`0xFFE4`, маркер `0xAA`,
  идентичности не несут). Разбор формата кадра и каталога — Этапы C-E.
- **Вывод:** PROTOCOL.md §1 → 🟢; §3 (авторизация) заполнен, статус 🟢.

### Шаг 004 — Гипотезы по формату кадра и объектам (анализ материала №4)
- **Тип:** аналитический шаг (новых материалов не снималось; разбор материала №4).
- **Инструменты:** `tools/hci_frames.py` (сбор кадров, реассемблирование ответа по длине, проверка
  CRC), `tools/hci_auth.py`, `tools/hci_topology.py`.
- **Наблюдения (все — 🟡 предположение, один захват):**
  - **Формат кадра.** Команда: `35 | длина | поле2 | класс | код | индекс | APEX+серийник | [payload] | CRC`.
    Ответ: `AA | длина | поле2 | класс(эхо) | код(эхо) | индекс записи | данные | CRC`. Ответы
    идентичности не несут. Детали — PROTOCOL.md §2.
  - **CRC-16/MODBUS**, little-endian — проверено на 406 кадрах обоих направлений (12 «плохих» —
    периодические `a5/03`, см. §2.5).
  - **Метка времени** — 6 байт `YY MM DD HH MM SS` (hex): `1a 09 10 10 16 14` = 2026-09-16
    16:22:20, совпадает со временем захвата (независимая сверка «радио↔время»).
  - **Каталог объектов** (класс `a3`): `31` статус-инициализация, `00` основной статус, `0c`/`0a`
    краткий статус/настройки, `0b` расписание, `08` таблица из 8 записей (предпол. базальные
    профили), `07` крупная таблица (предпол. настройки болюса), `21` и `26` — журналы по 10
    записей с метками времени. Класс `a5/03` — периодический (предпол. keepalive). Детали —
    PROTOCOL.md §4.
  - **Управление** (класс `a1`): `31` — установка времени помпы (payload = метка времени, равная
    времени подключения → синхронизация); квитанция `a1/55`. PROTOCOL.md §5.
- **Достоверность:** статус всех выводов — 🟡, так как получены из одного захвата. План перевода в
  🟢 (правило 8): второй захват с **иными значениями** — другое время (проверка позиций полей метки
  времени), другой базальный профиль/настройки болюса (привязка объектов `08`/`07`), и сверка
  числовых полей статуса (`00`/`0c`) с экраном помпы (батарея, резервуар, базал).
- **Вывод:** PROTOCOL.md §2, §4, §5 заполнены гипотезами (🟡). Формат кадра и CRC — приоритет на
  подтверждение в Этапе C.

### Шаг 005 — Карта возможностей приложения и «пульс» помпы
- **Дата/время:** 2026-09-16, ~17:45 (скриншоты меню).
- **Исследования:** `app-menu-map`; анализ пульса — по материалу №4.
- **Действие:** пользователь прошёл по меню приложения и сделал скриншоты (материал №7). Обмена с
  помпой для карты меню не требовалось. Анализ пульса — разбор ранее снятого материала №4.
- **Материалы:** №7 (5 скриншотов меню); №4 (HCI, для пульса).
- **Карта меню:** составлена в `APP_MENU.md` — экраны Главная/История/Устройства/Настройки болюса/
  Настройки базала/Вспомогат. функции; выделены группы извлечения G1–G6.
- **Корроборация гипотез (🟡):** меню «Настройки базала» (профиль A…, 48-интервальный график)
  косвенно подтверждает, что объект `a3/08` — базальные профили (8 × 48). Кнопки Главной
  (Еда/Болюс/Совет/ВБС/Стоп) и «Опасная зона» задают перечень управляющих команд для Этапа E.
- **«Пульс» помпы (`a5/03`):** `tools/hci_pulse.py` по материалу №4 — 12 кадров со **строгим шагом
  180,0 с**, полезная нагрузка **идентична** во всех (`aa0600a5030080c2`), CRC-16/MODBUS верна.
  Это незапрошенный периодический heartbeat помпы. Уточнил §2.5: длина `a5` = `total − 2`.
- **Достоверность:** пульс получен из одного захвата (🟡); подтверждение — второй захват (группа G6,
  удержание соединения ≥10 мин): период и постоянство нагрузки должны повториться.
- **Вывод:** добавлен `APP_MENU.md`; PROTOCOL.md §4 (a5/03 heartbeat, a3/08 базал-профили) и §2.5
  уточнены; определён план групп извлечения G1–G6.

### Шаг 006 — G1 Статус: поля объектов `a3/00` и `a3/0c`
- **Дата/время:** 2026-09-16, ~18:01–18:02.
- **Исследование:** `etap-D-g1-status`.
- **Действие:** пользователь открыл экраны «Главная» и «Устройства» и сделал скриншоты; помпа
  подключена, приложение опрашивало статус. Захват HCI снят сразу после. Управление — вручную,
  своих кадров не отправляли.
- **Материалы:** №8 (HCI, статус 18:01–18:02), №9 (2 скриншота: Главная IOB 1,87; Устройства
  батарея 1,36 В/46%, серийник замаскирован). Разбор: `tools/hci_frames.py` + анализ полей.
- **Наблюдения:**
  - **Метка времени** `a3/00` off44–49 = `1a 09 10 12 02 00` = 2026-09-16 18:02:00 — совпала с
    часами. С учётом ряда значений из Шага 004 (16:22:20…16:29:00) — **🟢**.
  - **Текущий базал** `a3/00` off76 (u16LE) = `20`; 20 × **0,025 Ед** = 0,5 Ед/ч — совпадает с
    экраном (базал 0,5 Ед/ч). Значение стабильно → **🟡** (подтвердить изменением базала, G2/G5).
  - **Напряжение батареи** `a3/0c` байт[3] = `136`; ×0,01 = 1,36 В — совпадает с экраном 18:01
    (g1_02) и фото помпы 16:52 (материал №6), две сессии → **🟢**.
  - **IOB** (1,87 Ед в 18:01; 2,33 в 17:44) в `a3/00` не найден ни в одном масштабе →
    предположительно **вычисляется приложением** из истории болюсов (🟡).
  - **Резервуар** (101,7 Ед) в `a3/00`/`a3/0c` не локализован — открытый вопрос (кандидат `a3/07`).
  - **Шаг дозирования 0,025 Ед** подтверждён на радио (базал 20 шагов = 0,5 Ед/ч).
- **Достоверность:** метка времени и напряжение батареи — 🟢 (ряд значений/две сессии + сверка
  радио↔экран). Базал — 🟡 (значение не менялось); IOB/резервуар — открыты.
- **Вывод:** PROTOCOL.md §4.1–4.3 заполнены. Дальше: G2 (смена базального профиля) закрепит базал
  и объект `a3/08`; поиск резервуара — в `a3/07` (G3).

### Шаг 007 — G1: классификация изменяющихся полей `a3/00` (анализ материалов №4/№8)
- **Тип:** аналитический шаг (без новых материалов).
- **Граница исследования:** учитываются только кадры **с 16:34** — рубеж, с которого ведутся
  подсчёты и захваты. Данные до 16:34 валидны, но отражают операции пользователя (болюсы и пр.),
  выполненные до начала исследования и не входящие в задокументированные шаги; они задали начальное
  состояние помпы, но нашими действиями не являются.
- **Рамка (задана пользователем):** изменяющиеся со временем поля — это метки времени или
  инсулиновые величины (резервуар, счётчик подачи, базал); поля, меняющиеся после операций, —
  состояния.
- **Результаты (окно ≥16:34):**
  - **Резервуар** = off52 (uint32 LE), единица 0,001 Ед: **16:52:06 → `101700` = 101,7 Ед**
    (совпадает с фото помпы, материал №6); 18:05 → 101050 ≈ 101 Ед (Главная, материал №9) → **🟢**.
  - **Счётчик подачи** = off16 (шаг 0,025 Ед): в чистом окне 16:52→18:05 прирост +0,625 Ед ≈ убыли
    резервуара (0,65 Ед) при базале без операций → 🟡.
  - **Базал** off76 = 20 (0,5 Ед/ч, шаг 0,025) — стабилен → 🟡.
  - **Метка времени** off44-49 → 🟢; **напряжение батареи** `a3/0c[3]`=136=1,36 В → 🟢 (Шаг 006).
  - **Поля состояния** off50/51/80/82/84/86: ненулевые до ~16:47, затем нулевые — затухающий
    «хвост» операций пользователя **до 16:34**; в каталог полей помпы не вносятся.
- **КОРРЕКЦИЯ прежней версии Шага 007.** Сняты как ошибочные: (1) отнесение полей состояния к
  «болюсам, сделанным в исследовании» — болюсы до 16:34 выполнял пользователь вне задокументированных
  шагов; (2) вывод о «внутренней сходимости» на интервале 16:22→18:05 — анализ был ошибочно расширен
  на до-исследовательский период. Прежний «открытый вопрос» о расхождении убыли резервуара снят:
  в чистом окне (после затухания пред-16:34 операций, ~16:47) убыль резервуара согласуется с
  базалом.
- **Методическая поправка (правило проекта):** извлечение по группам анализируем строго в окне
  исследования (**с 16:34**) и в окне соответствующего контролируемого захвата, а не по всей
  истории лога.
- **Вывод:** PROTOCOL.md §4.1 приведён к окну ≥16:34: резервуар 🟢; счётчик подачи и базал 🟡;
  поля состояния отнесены к пред-исследовательскому контексту.

### Шаг 008 — Промежуточная карта разбора объектов (`OBJECTS.md`)
- **Тип:** аналитический/инструментальный шаг (без новых материалов).
- **Что сделано:** добавлен `tools/object_map.py` и документ `OBJECTS.md` — для каждого объекта
  ответа (`a3/00,0c,31,0a,0b,08,07,21,26`, `a5/03`) пословная карта payload с сырыми значениями,
  отметкой «константа/переменная» и текущей расшифровкой (🟢/🟡/🔲). Цель — чтобы выводы можно было
  проверять по сырым данным, а не принимать на слово.
- **Новые кандидаты в `a3/00`** (сверка с экранами приложения, материал №7): off24 `320` → 8,0 Ед =
  «Максимальный базал» (menu_04); off26 `480` → 12,0 Ед = «Максимальный болюс» (menu_03). Статус 🟡
  — подтверждение: изменить лимит в приложении и увидеть изменение поля.
- **Разбор `a3/00`:** 18/88 байт (метка времени 🟢, резервуар 🟢, счётчик подачи 🟡, базал 🟡,
  макс. базал/болюс 🟡). Прочие константы (off0=259, off8=2826, off14=150, off20=100, off28=140,
  off30=76, off32=92, off78=6402) — не разобраны, кандидаты в настройки/лимиты (проверяются
  изменением в приложении).
- **Примечание:** объекты `08/07/21/26/0a/0b/31` читались приложением при подключении до 16:34 —
  их карты приведены по до-исследовательским кадрам и требуют повторного захвата в окне.
- **Вывод:** `OBJECTS.md` — рабочая карта для дальнейшего разбора полей.

### Шаг 009 — G3/E: подтверждение «макс. болюс» и команда записи настроек `a1/32`
- **Дата/время:** 2026-09-16, ~19:53 (после изменения настройки пользователем).
- **Исследование:** `etap-E-zapis-nastroek`.
- **Действие:** пользователь в приложении установил **максимальный болюс 12,3** (было 12,0).
  Приложение записало настройку в помпу и перечитало статус. Захват HCI — материал №10.
- **Результаты:**
  - **`a3/00` off26 = 492** (было 480); ×0,025 = **12,3 Ед** (было 12,0). Второе измерение с иным
    значением → **максимальный болюс подтверждён (🟢)**.
  - Управляющая команда **`a1/32`** (запись настроек): payload заканчивается `40 01 ec 01` =
    макс. базал 320 (8,0 Ед) + макс. болюс 492 (12,3 Ед). То есть a1/32 несёт блок настроек,
    совпадающий с константами `a3/00`. → **🟢** для отображения макс. болюса; a1/32 внесён в §5.
  - **`a1/31`** (установка времени): наблюдались два разных значения метки времени (16:22:20 и
    16:52:06) → **🟢**.
  - Корроборация off24 (макс. базал 320=8,0): присутствует в блоке a1/32 (значение перед макс.
    болюсом), но само значение не менялось → остаётся 🟡 до изменения лимита.
- **Достоверность:** макс. болюс — 🟢 (два значения 12,0/12,3 + то же в команде a1/32). Команды
  a1/31, a1/32 — 🟢. a1/33, a1/34 — 🟡 (назначение не установлено).
- **Вывод:** PROTOCOL.md §4.1 (off26 🟢), §5 (a1/31, a1/32 🟢); OBJECTS.md перегенерирован.

### Шаг 010 — подтверждение «макс. базал» (off24)
- **Дата/время:** 2026-09-16, ~20:08. **Исследование:** `etap-E-zapis-nastroek`. **Материал:** №11 (HCI).
- **Действие:** пользователь установил **максимальный базал 7,7** (было 8,0).
- **Результат:** `a3/00` off24: 320 → **308** (×0,025 = 7,7 Ед); команда `a1/32` показывает `34 01`
  (308) в позиции макс. базала. Два значения (8,0/7,7) → **off24 = максимальный базал 🟢**.
- **Вывод:** PROTOCOL.md §4.1 (off24 🟢); OBJECTS.md перегенерирован; блок настроек a1/32 —
  последние два u16 (макс. базал, макс. болюс) подтверждены.

### Шаг 011 — Серия настроек болюса/звука: поля команд `a1/32` и `a1/34`
- **Дата/время:** 2026-09-16, 20:16–20:18. **Исследование:** `etap-E-nastroiki-boljus`.
  **Материалы:** №12 (HCI, серия), №13 (4 скриншота «Настройки болюса»).
- **Метод:** пользователь по очереди менял настройки; из лога извлечена последовательность команд
  `a1/32`/`a1/34`, разности соседних команд сопоставлены с порядком действий и скриншотами.
- **Действия ↔ команды (все подтверждены сменой значения + экраном):**
  - расширенный болюс ВКЛ → `a1/32` байт[6] 00→01 → 🟢;
  - скорость болюса Нормальная→Низкая → `a1/32` байт[0] 00→01 → 🟢 (энум частичный);
  - звуковой режим ВКЛ → `a1/34` байт[0] 00→01 → 🟢;
  - шаг звукового режима 5,0→0,5→0,1 → `a1/34` байт[1] 03→01→00 (индекс; 3 значения) → 🟢.
- **Наблюдение:** при каждом изменении настройки приложение шлёт оба блока `a1/32` и `a1/34`;
  также замечены `a1/12` (`12 00 00`) и `a1/05` (без нагрузки) — назначение не установлено (🟡).
- **Достоверность:** для каждого поля — два состояния (до/после) + сверка со скриншотом (s3/s4,
  материал №13). Шаг звука — 3 значения.
- **Вывод:** PROTOCOL.md §5.1 (блок `a1/32`) и §5.2 (блок `a1/34`) заполнены.

### Шаг 012 — Напоминание о ГК (флаги `a1/32`) и предустановки болюса (`a1/11`)
- **Дата/время:** 2026-09-16, 22:42–22:43. **Исследование:** `etap-E-gk-presety`.
  **Материалы:** №14 (HCI), №15 (2 скриншота «Предустановки болюса»).
- **Напоминание о ГК:** `a1/32` байт[6] `01→03`. Поскольку bit0 = расширенный болюс, байт[6] —
  **битовое поле**: bit0=расширенный болюс, bit1=напоминание о ГК → 🟢.
- **Предустановки болюса — команда `a1/11`:** список u16LE (шаг 0,025 Ед) по слотам. Финал
  `84 00 54 00 50 00 05 00 03 00 …` = 3,3 / 2,1 / 2,0 / 0,125 / 0,075 / 0,0 Ед — точное совпадение
  с экраном «Предустановки болюса» (материал №15) → 🟢. При правке пересылается весь список.
- **Прочее (Этап E):** далее в захвате — команды `a1/02` (`01 08 00 00`, `01 02 20 00`…), `a1/12`
  (`0c/12/14 00 00`); назначение не установлено, требуют отдельного описания действий.
- **Вывод:** PROTOCOL.md §5 (a1/11), §5.1 (байт[6]=флаги), §5.3 (предустановки) заполнены.

### Шаг 013 — Серия «Дополнительных настроек» (`a1/32`): частичная атрибуция
- **Дата/время:** 2026-09-16, 23:12–23:21. **Исследование:** `etap-E-dop-nastroiki`.
  **Материалы:** №16 (HCI + `a1_32_evolution.txt`), №17 (9 скриншотов).
- **Метод и оговорка:** пользователь менял большую серию настроек подряд (часть — с откатами) без
  захвата между изменениями. Поэтому атрибуция «байт↔настройка» надёжна только там, где **заданное
  число совпало с байтом**; флаговые/энумные байты по одному не разведены — не угадываются.
- **Подтверждено (🟢, по совпадению значений):**
  - байт[3] = авто-выключение помпы, часы (01→07 = 1→7 ч);
  - байт[4] = «инсулин заканчивается» порог, единицы (0a→0d = 10→13);
  - байт[5] = «инсулин заканчивается» время, получасы (0b→0d = 5:30→6:30);
  - байты[8..9] = автовыключение экрана, ×0,1 с (150↔600 = 15↔60 с);
  - байты[10..11] = лимит дневной дозы, единицы (64→fa = 100→250).
- **Уточнение:** байт[6] подтверждён ранее (bit0 расш.болюс, bit1 напоминание о ГК); байт[0] —
  битовое поле (bit0 = скорость болюса, Шаг 011; старшие биты — прочие переключатели, не разведены).
- **Не разведено (🟡):** байт[0] старшие биты, байт[1], байт[2], байт[7] — соответствуют
  переключателям/энумам (блокировка клавиш, язык, яркость, длительность/вид тревоги, лимит дозы
  вкл). Требуют по одному изменению с отдельным захватом; сырьё — в `a1_32_evolution.txt` (№16) и
  скриншотах (№17).
- **Методическая поправка (важно):** для флаговых/энумных настроек делаем **одно изменение —
  один захват**, иначе однозначная привязка невозможна.
- **Отражение в объекте `a3/00`:** те же настройки видны и в статус-объекте (проверено по логу
  серии): off14 (150↔600) = автовыключение экрана; off20 (100↔250) = лимит дневной дозы;
  off28/30/32 = предустановки болюса (слоты 1–3). Внесены в `OBJECTS.md` (a3/00: 28/88 байт).
- **Вывод:** PROTOCOL.md §5.1 дополнен (5 полей 🟢); `OBJECTS.md` (a3/00) обновлён; флаги/энумы —
  🟡 с сохранённым сырьём.

### Шаг 014 — Скорость болюса Низкая→Нормальная: обратное направление, отражение в `a3/00`
- **Дата/время:** 2026-09-16, ~23:46. **Исследование:** `etap-E-skorost-boljusa`.
  **Материалы:** №18 (HCI), №19 (скриншот «Настройки болюса» после смены).
- **Действие:** пользователь переключил «Скорость болюса» из «Низкая» в «Нормальная» (одно изменение).
- **Команда `a1/32`:** последняя в логе команда отличается от предыдущей только байтом[0]: `01→00`
  (`…010200070b0d03029600fa003401ec01` → `…000200070b0d03029600fa003401ec01`). Вместе с Шагом 011
  (`00→01`) bit0 подтверждён в обоих направлениях → 🟢.
- **Объект `a3/00`:** сравнение статуса до/после каждой команды `a1/32` — после этой команды меняется
  только off2 `1→0`; в логе Шага 011 (№12) при Нормальная→Низкая (20:16) — off2 `0→1`. Других значений
  off2 в обоих логах нет → **off2 = скорость болюса (0=Нормальная, 1=Низкая)** 🟢. Соседний off3 сюда
  не относится (менялся в серии Шага 013).
- **Скриншот:** экран «Настройки болюса» показывает «Скорость болюса — Нормальная» (№19).
- **Инструмент:** `tools/object_map.py` выводит однобайтовые поля с выравниванием u16-сетки; заодно
  исправлен пропуск известного поля на нечётном смещении (в `OBJECTS.md` у `a3/0c` строка off3
  «напряжение батареи» поглощалась строкой `2..3`).
- **Вывод:** PROTOCOL.md §4.1 (off2 🟢) и §5.1 (байт[0] bit0 — оба направления); `OBJECTS.md` (a3/00: 29/88,
  a3/0c: строка off3 восстановлена).

### Шаг 015 — Расширенный болюс и напоминание о ГК ВЫКЛ: флаги `a1/32` байт[6] и `a3/00` off4
- **Дата/время:** 2026-09-16 ~23:55 и ~23:59. **Исследование:** `etap-E-flagi-boljusa`.
  **Материалы:** №20 (HCI), №21 (2 скриншота «Настройки болюса», по одному после каждого изменения).
- **Действия (по одному изменению, захват после каждого):** пользователь выключил «Расширенный болюс»
  (~23:55), затем «Напоминание о ГК» (~23:59).
- **Команда `a1/32`:** байт[6] `03→02` (сброшен bit0), затем `02→00` (сброшен bit1); прочие байты без
  изменений. С Шагами 011/012 (включение) оба бита подтверждены в обоих направлениях → 🟢.
- **Объект `a3/00`:** сравнение статуса до/после каждой команды `a1/32` — меняется только off4:
  | время | действие | `a1/32` байт[6] | `a3/00` off4 |
  |---|---|---|---|
  | 20:16 (Шаг 011, №12) | расш. болюс ВКЛ | `00→01` | `0→2` |
  | 22:42 (Шаг 012, №14) | напоминание о ГК ВКЛ | `01→03` | `2→6` |
  | ~23:55 | расш. болюс ВЫКЛ | `03→02` | `6→4` |
  | ~23:59 | напоминание о ГК ВЫКЛ | `02→00` | `4→0` |
  → **off4 — битовое поле: `0x02` = расширенный болюс, `0x04` = напоминание о ГК** 🟢. В `a3/00` биты
  на одну позицию старше, чем в команде; bit0 (`0x01`) в off4 не наблюдался (значения только 0/2/4/6).
- **Скриншоты:** №21 — оба переключателя выключены в соответствующем порядке.
- **Вывод:** PROTOCOL.md §4.1 (off4 🟢) и §5.1 (байт[6] — выключение); `OBJECTS.md` (a3/00: 30/88);
  `tools/object_map.py` KNOWN дополнен.

### Шаг 016 — Звуковой режим ВЫКЛ: `a1/34` байт[0] и отражение в `a3/0c` off4/off5
- **Дата/время:** 2026-09-17, ~00:11. **Исследование:** `etap-E-zvukovoi-rezhim`.
  **Материалы:** №22 (HCI), №23 (скриншот «Настройки болюса» после смены).
- **Действие:** пользователь выключил «Звуковой режим» (одно изменение). На экране при выключении
  скрывается строка «Шаг звукового режима».
- **Команда `a1/34`:** байт[0] `01→00`, прочие байты без изменений (байт[1]=`00`, шаг 0,1 сохранён).
  С Шагом 011 (`00→01`) подтверждено в обоих направлениях → 🟢.
- **Объект `a3/0c`:** в `a3/00` звуковых настроек нет (после команд `a1/34` не меняется). В кратком
  статусе `a3/0c` off4/off5 меняются только вслед за командами `a1/34`:
  | время | команда `a1/34` [0..1] | `a3/0c` off4..5 |
  |---|---|---|
  | 20:17 (Шаг 011, №12) | `01 03` (звук ВКЛ) | `00 03` → `01 03` |
  | 20:17–20:18 (Шаг 011) | `01 01`, `01 00` (шаг 5,0→0,5→0,1) | `01 01`, `01 00` |
  | 00:11 | `00 00` (звук ВЫКЛ) | `00 00` (00:12) |
  → **off4 = звуковой режим (0/1)** 🟢 (оба направления); **off5 = шаг звукового режима, индекс как в
  `a1/34` байт[1]** 🟢 (3 значения, как и сама команда в Шаге 011).
- **Вывод:** PROTOCOL.md §4.2 (off4, off5 🟢) и §5.2 (выключение, отражение в `a3/0c`); `OBJECTS.md`
  (a3/0c: 3/20); `tools/object_map.py` KNOWN дополнен.

### Шаг 017 — Вид сигнала тревоги: `a1/32` байт[1] и `a3/00` off1
- **Дата/время:** 2026-09-17, ~00:18 и ~00:23. **Исследование:** `etap-E-vid-signala`.
  **Материалы:** №24 (HCI), №25 (2 скриншота «Сигналы тревоги»); сверка — №16/№17 (Шаг 013).
- **Действия (по одному изменению, захват после каждого):** пользователь переключил «Вид сигнала»
  на «Вибрация» (~00:18), затем на «Звуковой» (~00:23). Меню предлагает три варианта: Звуковой,
  Вибрация, Звуковой и вибрация (скриншот №25/01); со слов пользователя индексы идут по порядку меню.
- **Команда `a1/32`:** байт[1] `02→01` (Вибрация), затем `01→00` (Звуковой); прочие байты без изменений.
- **Объект `a3/00`:** off1 `2→1` (00:19), `1→0` (00:24) — только этот байт.
- **Третье значение:** в Шаге 013 байт[1] стал `02` в 23:17 и держался до 00:18; скриншот №17 `adv_09`
  (23:19) показывает «Звуковой и в…» → `2` = Звуковой и вибрация.
- **Итог:** **байт[1] `a1/32` = off1 `a3/00` = вид сигнала тревоги: 0=Звуковой, 1=Вибрация, 2=Звуковой
  и вибрация** 🟢 — все три значения сверены экран↔эфир, совпадает с порядком меню.
- **Попутная сверка:** на скриншоте №25/02 «Инсулин заканчивается — единицы» = 11 Ед, в команде байт[4]=`0b`
  (=11) — согласуется с Шагом 013.
- **Вывод:** PROTOCOL.md §4.1 (off1 🟢) и §5.1 (байт[1] из 🟡 в 🟢); `OBJECTS.md` (a3/00: 31/88).

### Шаг 018 — Длительность сигнала тревоги: `a1/32` байт[7] и `a3/0c` off2
- **Дата/время:** 2026-09-17, ~00:29. **Исследование:** `etap-E-dlitelnost-signala`.
  **Материалы:** №26 (HCI), №27 (скриншот с меню вариантов); сверка — №17 `adv_09` (Шаг 013).
- **Действие:** пользователь переключил «Длительность сигнала» Короткий→Длинный (одно изменение).
  Меню: Длинный, Нормальный, Короткий (№27); со слов пользователя индексы идут по порядку меню.
- **Команда `a1/32`:** байт[7] `02→00`, прочие байты без изменений.
- **Объект `a3/0c`:** off2 `2→0` (00:29). В `a3/00` изменений нет.
- **История байта по логу (№26):**
  | время | `a1/32` байт[7] | `a3/0c` off2 | экран |
  |---|---|---|---|
  | 20:20 | — | `2` | (Короткий — см. 00:25, №25/02) |
  | 23:15–23:16 (Шаг 013) | `1` | `1` | 23:19 «Нормаль…» (№17 `adv_09`) |
  | 23:20 (Шаг 013) | `2` | `2` | 00:25 «Короткий» (№25/02) |
  | 00:29 | `0` | `0` | 00:31 «Длинный» (№27) |
- **Итог:** **байт[7] `a1/32` = off2 `a3/0c` = длительность сигнала тревоги: 0=Длинный, 1=Нормальный,
  2=Короткий** 🟢 — все три значения сверены экран↔эфир, совпадает с порядком меню.
- **Уточнение к Шагу 013:** байт[7] ранее был кандидатом «длительность/вид тревоги» (🟡) — закрыт.
  Байт[2] к длительности не относится: он сменился `04→00` в 23:21, когда байт[7] оставался `2`
  (Короткий) — назначение байта[2] не установлено, 🟡.
- **Вывод:** PROTOCOL.md §4.2 (off2 🟢) и §5.1 (байт[7] из 🟡 в 🟢); `OBJECTS.md` (a3/0c: 4/20).

### Шаг 019 — Яркость экрана помпы: `a1/32` байт[2] и `a3/00` off3
- **Дата/время:** 2026-09-17, ~00:41. **Исследование:** `etap-E-yarkost-ekrana`.
  **Материалы:** №28 (HCI), №29 (скриншот с меню вариантов); сверка — №17 `adv_01`, `adv_05` (Шаг 013).
- **Действие:** пользователь переключил «Яркость экрана» (Дополнительные настройки) 10%→50% (одно
  изменение). Меню: 10, 30, 50, 60, 80, 100 % (№29).
- **Команда `a1/32`:** байт[2] `00→02`, прочие байты без изменений.
- **Объект `a3/00`:** off3 `0→2` (00:41).
- **Сверка трёх значений с экраном:**
  | время | `a1/32` байт[2] | `a3/00` off3 | экран |
  |---|---|---|---|
  | 23:12 (Шаг 013) | `00` | `0` | 10 % (№17 `adv_01`) |
  | 23:15 (Шаг 013) | `04` | `4` | 80 % (№17 `adv_05`) |
  | 00:41 | `02` | `2` | 50 % (№29) |
- **Итог:** **байт[2] `a1/32` = off3 `a3/00` = яркость экрана, индекс позиции в меню** (0=10%, 1=30%,
  2=50%, 3=60%, 4=80%, 5=100%) 🟢 — три значения сверены экран↔эфир и совпадают с позициями меню;
  значения 1/3/5 выведены из порядка меню.
- **Попутно (возврат настроек ~00:37–00:38):** `a3/0c` off2 `0→2` (длительность → Короткий) и `a1/32`
  байт[1] `0→1` (вид → Вибрация) — согласуется с Шагами 017/018. Кроме того, в 00:38 бит `0x02` байта[0]
  включался и выключался (`a3/00` off5 `0→1→0`) — действие пользователя уточняется, 🟡.
- **Вывод:** PROTOCOL.md §4.1 (off3 🟢) и §5.1 (байт[2] из 🟡 в 🟢); `OBJECTS.md` (a3/00: 32/88).
