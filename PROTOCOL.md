# Техническое описание протокола помпы TruCare III

Описание **протокола самой помпы**: транспорт, формат кадра, управляющие команды, которые помпа
принимает, и объекты ответа, которые она отдаёт. Обмены инициируются штатным приложением
Erwin Blueberry (используется по назначению); предметом описания является поведение помпы, а не
приложения. Документ строится **только** на материалах этого проекта; каждое утверждение
сопровождается ссылкой на шаг в `RESEARCH.md` и статусом достоверности.

Легенда статусов: 🟡 предположение (одно измерение) · 🟢 подтверждено (≥2 измерения с различными
характеристиками, правило 8) · 🔴 требует перепроверки.

Идентификационные данные, адреса, серийные номера и пароли не приводятся — на их месте
плейсхолдеры (`<identity>`, `<serial>`, `<crc>`).

---

## 1. Транспорт

BLE GATT. Помпа выступает GATT-сервером, телефон со штатным приложением — клиентом. Адрес помпы
здесь не приводится (`<address>`). Источник: RESEARCH.md, Шаг 001 и Шаг 003 (материалы №1, №2, №4-6).
**Статус: 🟢 подтверждено** — два независимых измерения: Шаг 001 (conn 0x0003) и Шаг 003
(conn 0x0005, переподключение после штатного отключения). Службы, характеристики, handle, MTU и
порядок включения CCCD совпали побайтово.

### 1.1. Службы GATT
| Служба | Диапазон handle | Назначение (предварительно) |
|---|---|---|
| `0x1800` | 0x0001–0x000A | Generic Access (стандартная) |
| `0x1801` | 0x000B–0x000E | Generic Attribute (стандартная) |
| `0x180A` | 0x000F–0x0019 | Device Information (стандартная) |
| `0xFFE0` | 0x001A–0x001E | Канал уведомлений помпы (ответы) |
| `0xFFE5` | 0x001F–0x0022 | Канал записи команд в помпу |
| `0xFF90` | 0x0023–0x003E | Вендорская служба (FF91–FF99); трафик команд/ответов не наблюдался |
| `0xFFC0` | 0x003F–…      | Канал предположительной авторизации (см. §3, Этап B) |

### 1.2. Характеристики протокола
- **Ответы помпы (помпа → телефон):** характеристика `0xFFE4`, value-handle `0x001C`, свойство
  `NOTIFY`. Именно сюда приходят кадры ответов; за сессию подключения наблюдалось множество
  уведомлений на этом handle.
- **Команды в помпу (телефон → помпа):** характеристика `0xFFE9`, value-handle `0x0021`,
  свойства `WRITE` / `WRITE_NR`.
- **Канал `0xFFC0`:** запись `0xFFC1` (value `0x0041`, `WRITE`) и уведомление `0xFFC2`
  (value `0x0044`, `NOTIFY`) — предположительно авторизация; подробнее в §3 (Этап B).
- Служба `0x180A` и вендорская `0xFF90` (FF91–FF99) несут информацию об устройстве и вендорские
  поля; протокольный обмен командами/ответами по ним не наблюдался.

### 1.3. Согласование MTU
Клиент запрашивает ATT MTU `517`; помпа выдаёт `251`.

### 1.4. Включение уведомлений
Уведомления включаются записью `01 00` в CCCD: `@0x001D` (для `0xFFE4`) и `@0x0045`
(для `0xFFC2`).

## 2. Формат кадра

**Статус: 🟡 предположение** (RESEARCH.md, Шаг 004; один захват, материал №4). Ниже — гипотезы по
структуре, подтверждённые внутренней согласованностью и проверкой CRC, но требующие второго
захвата с иными значениями для перевода в 🟢. Идентификационный блок команд показан как
`<identity>`.

### 2.1. Кадр команды (телефон → помпа, характеристика `0xFFE9`)
| Смещение | Размер | Поле | Примечание |
|---|---|---|---|
| 0 | 1 | маркер | всегда `0x35` |
| 1 | 1 | длина | длина всего кадра в байтах |
| 2 | 1 | поле-2 | наблюдалось `0x00`; назначение не установлено |
| 3 | 1 | класс | `0xA3` — чтение объекта; `0xA1` — управление/запись |
| 4 | 1 | код объекта/операции | напр. `0x00`, `0x08`, `0x21`, `0x26`, `0x31`, `0x33` |
| 5 | 1 | индекс/селектор | `0xAA` — без индекса; иначе конкретный индекс/страница |
| 6..17 | 12 | идентификация | ASCII `APEX`+серийный номер (`<identity>`) |
| 18..N-3 | var | полезная нагрузка | для команд с параметром (напр. установка времени) |
| N-2..N-1 | 2 | CRC | CRC-16/MODBUS, little-endian |

Кадр без параметра — 20 байт (6 заголовка + 12 идентификации + 2 CRC).
Пример (чтение статуса): `35 14 00 a3 00 aa <identity> 98 86`.

### 2.2. Кадр ответа (помпа → телефон, характеристика `0xFFE4`)
| Смещение | Размер | Поле | Примечание |
|---|---|---|---|
| 0 | 1 | маркер | всегда `0xAA` |
| 1 | 1 | длина | длина всего кадра; кадр может занимать несколько уведомлений |
| 2 | 1 | поле-2 | назначение не установлено |
| 3 | 1 | класс | эхо класса команды (`0xA3`/`0xA1`/`0xA5`) |
| 4 | 1 | код объекта | эхо кода команды |
| 5 | 1 | индекс записи | `00,01,02,…` для многозаписных объектов; `0xAA` для одиночных |
| 6..N-3 | var | данные объекта | **идентичности не несёт** |
| N-2..N-1 | 2 | CRC | CRC-16/MODBUS, little-endian |

- Длинный ответ приходит несколькими уведомлениями по `0xFFE4` и собирается по полю длины.
- **Квитанция управления:** класс `0xA1`, код `0x55` (`A1/55`) — подтверждение команды записи;
  команду по имени не называет.

### 2.3. Контрольная сумма
**CRC-16/MODBUS** (полином `0x8005`, init `0xFFFF`, reflected in/out, xorout `0x0000`),
дописывается **little-endian**. Проверено на 406 кадрах обоих направлений (команды, ответы, ACK).

### 2.4. Метка времени (общее поле)
Шесть байт `YY MM DD HH MM SS` (обычные hex-байты, не BCD). Пример: `1a 09 10 10 16 14` =
2026-09-16 16:22:20 (сверено со временем захвата). Встречается в объектах статуса и журналов и как
полезная нагрузка команды установки времени (§5).

### 2.5. Открытый вопрос по полю длины
Для классов `A3`/`A1` поле `[1]` = полный размер кадра (включая CRC) — проверено CRC на 406 кадрах.
Для периодического `A5/03` поле `[1]` = `0x06` = размер кадра **за вычетом 2-байтового заголовка**
(маркер+длина), т.е. `total − 2`; CRC-16/MODBUS при этом корректна (тело `aa0600a50300`, CRC
`80c2`). Единая трактовка базы отсчёта длины между классами не установлена — уточнить отдельным
измерением.

## 3. Установление сессии и авторизация

**Статус: 🟢 подтверждено** (RESEARCH.md, Шаг 001 и Шаг 003; материалы №4-6). Помпа требует
авторизации паролем на каждом подключении.

### 3.1. Выбор помпы
Подключение инициируется выбором помпы по её **серийному номеру**: пользователь вводит серийный
номер → помпа появляется в списке → тап по строке → подключение.

### 3.2. Пароль (Bluetooth-пароль помпы)
Источник пароля — экран помпы «СОСТОЯНИЕ», строка `B/P:` (сразу после строки `S/N:`). Это
**шесть десятичных цифр**.

### 3.3. Последовательность на подключении
1. Включение уведомлений вердикта: запись `01 00` в CCCD `0x0045` (характеристика `0xFFC2`).
2. Запись пароля в `0xFFC1` (value `0x0041`): **12 байт** — шесть ASCII-цифр пароля, записанных
   дважды подряд (значение не приводится, `<password>`).
3. Вердикт: уведомление на `0xFFC2` (value `0x0044`), **1 байт**; `0x00` — успех (обе сессии).
4. Согласование MTU (§1.3).
5. Включение уведомлений ответов: запись `01 00` в CCCD `0x001D` (характеристика `0xFFE4`).
6. Далее — команды/ответы (§4-§5).

### 3.4. Достоверность
Структура подтверждена двумя сессиями (conn `0x0003`, conn `0x0005`) и независимой сверкой
радио↔экран помпы: ASCII-цифры в записи `0xFFC1` совпадают со строкой `B/P:` на экране помпы
(материал №6). Кодирование «шесть цифр дважды» установлено для одного пароля; смена пароля
(функция приложения «Сменить пароль Bluetooth») даст дополнительную проверку — возможное будущее
измерение.

## 4. Объекты ответа (чтение)

**Статус: 🟡 предположение** (RESEARCH.md, Шаг 004; один захват, материал №4). Назначения полей —
гипотезы; подтверждение — в Этапе D сверкой «радио↔экран» при разных значениях. Класс `0xA3`.
Полная побайтовая карта разбора каждого объекта (со степенью разбора) — в `OBJECTS.md`.

| Код | Записей | Размер | Гипотеза назначения |
|---|---|---|---|
| `0x31` | 1 | 16 | инициализация сессии/статус после авторизации (`0000000001010104`) |
| `0x00` | 1 | ~96 | основной статус помпы; поля — §4.1 (метка времени 🟢, текущий базал 🟡) |
| `0x0C` | 1 | 28 | краткий статус; поля — §4.2 (напряжение батареи 🟢) |
| `0x0A` | 1 | ~28 | настройка/пара (две метки времени) |
| `0x0B` | 1 | ~34 | расписание/набор записей (токены `1a0910 02…`) |
| `0x08` | 8 (idx 00–07) | 104 | базальные профили: 8 профилей × 48 получасовых значений (16-бит LE); косвенно подтверждено меню «Настройки базала» (выбор профиля A…, график на 48 интервалов, материал №7) |
| `0x07` | 1 | 254 | крупная таблица «время–значение»; предположительно настройки болюса/коэффициентов |
| `0x21` | 10 (idx 00–09) | 22 | журнал событий с метками времени |
| `0x26` | 10 (idx 00–09) | 18 | журнал (значения + метки времени); предположительно история доз/измерений |

Отдельно, класс `0xA5`:
| `0x03` | периодический | 8 | «пульс»/heartbeat помпы: незапрошенный кадр, постоянная нагрузка `aa0600a5030080c2`, интервал **180 с** (12 подряд с шагом 180,0 с; RESEARCH.md Шаг 005). CRC-16/MODBUS верна (тело `aa0600a50300`) |

Значения многозаписных объектов (`0x08`, `0x21`, `0x26`) — 16-битные little-endian; конкретные
единицы и привязка к базалу/болюсу/истории устанавливаются в Этапе D.

### 4.1. Поля объекта `a3/00` (основной статус, 88 байт данных)
Анализ ведётся в окне исследования **с 16:34** (RESEARCH.md Шаг 007); более ранние кадры отражают
операции пользователя, выполненные до начала исследования, и в выводы не входят.

Таблица генерируется из реестра `tools/fields.py` и проверяется по архивным захватам (`tools/verify_fields.py`); вручную не правится.

<!-- gen:a3_00 -->
| Смещение | Поле | Кодирование | Команда | Статус |
|---|---|---|---|---|
| 0 (u8) | индикатор батареи (деления) | число делений; гипотеза: ступени по напряжению `a3/0c` off3 — 4→3 при 1,42 В, 3→2 при 1,34 В, 2→1 при 1,26 В | — | 🟡 — шаги 024; №20, №38; в окне `3` при 1,35–1,37 В (601 кадр) — согласуется; `2` лишь 23:59–00:02 (4 кадра, 1,35–1,36 В) при замене резервуара — возможно, просадка под нагрузкой мотора, не видимая в опросе; проверка — устойчивое `2` при ≤1,33 В |
| 1 (u8) | вид сигнала тревоги | `0`=Звуковой; `1`=Вибрация; `2`=Звуковой и вибрация (порядок пунктов меню, №25) | `a1/32` [1] | 🟢 — шаги 013, 017; №16, №17, №24, №25 |
| 2 (u8) | скорость болюса | `0`=Нормальная; `1`=Низкая | `a1/32` [0, бит `0x01`] | 🟢 — шаги 011, 014; №12, №13, №18, №19; меню целиком не снималось; наблюдались оба значения |
| 3 (u8) | яркость экрана помпы | `0`=10 %; `1`=30 %; `2`=50 %; `3`=60 %; `4`=80 %; `5`=100 % (порядок пунктов меню, №29) | `a1/32` [2] | 🟢 — шаги 013, 019; №16, №17, №28, №29; значение — индекс пункта меню |
| 4 (u8) | расширенный болюс / напоминание о ГК | битовое поле: bit0 = «Расширенный болюс» (разрешает растянутый и двойной болюс), bit1 = «Напоминание о ГК»; в `a3/00` off4 = байт × 2 (`00`/`02`/`04`/`06`) | `a1/32` [6] (статус = байт × 2) | 🟢 — шаги 011, 012, 015; №12, №13, №14, №20, №21 |
| 5 (u8) | блокировка клавиатуры | `0`=выкл; `1`=вкл | `a1/32` [0, бит `0x02`] | 🟢 — шаги 013, 023; №16, №17, №31, №36, №37 |
| 6 (u8) | автовыключение помпы | `0`=выкл; `1`=вкл | `a1/32` [0, бит `0x04`] | 🟢 — шаги 013, 022; №16, №17, №31, №34, №35, №37 |
| 7 (u8) | время автовыключения помпы | число, ч | `a1/32` [3] | 🟢 — шаги 013, 022; №16, №17, №34, №35; число часов, не индекс; в видимой части меню 2–8 ч (№35), 1 ч — на экране (№17); полный список не снят |
| 8 (u8) | «инсулин заканчивается» — единицы | число, Ед | `a1/32` [4] | 🟢 — шаги 013, 022; №16, №17, №25; число единиц; меню не снималось |
| 9 (u8) | «инсулин заканчивается» — время | число получасов (не индекс меню): `11`=05:30, `13`=06:30 | `a1/32` [5] | 🟢 — шаги 013, 022, 023; №16, №17, №25, №37; меню 02:00–12:00 шагом 0:30 (`4`…`24`) — со слов пользователя; на №37 видимы 02:00–05:00 |
| 13 (u8) | лимит дневной дозы | `0`=выкл; `1`=вкл | `a1/32` [0, бит `0x20`] | 🟢 — шаги 013, 021; №16, №17, №25, №32, №33, №37 |
| 14..15 (u16LE) | автовыключение экрана | × 0,1 с | `a1/32` [8..9 (u16LE)] | 🟢 — шаги 013; №16, №17, №31; меню не снималось; в эфире 150 и 600 |
| 16..17 (u16LE) | счётчик поданного инсулина | × 0,025 Ед | — | 🟢 — шаги 007, 024; №8, №38; прирост соответствует скорости подачи (0,5 и 0,9 Ед/ч, ВБС 6,0 Ед/ч); обнулился в 00:00 — «за сутки» 🟡 |
| 20..21 (u16LE) | общая дневная доза (лимит) | число, Ед | `a1/32` [10..11 (u16LE)] | 🟢 — шаги 013, 021, 022; №16, №17, №32, №33, №34; число единиц, не индекс; меню 50–300 шагом 50 (№33) |
| 24..25 (u16LE) | максимальный базал | × 0,025 Ед/ч | `a1/32` [12..13 (u16LE)] | 🟢 — шаги 010; №11; 8,0→7,7 (320→308) |
| 26..27 (u16LE) | максимальный болюс | × 0,025 Ед | `a1/32` [14..15 (u16LE)] | 🟢 — шаги 009; №10, №21; 12,0→12,3 (480→492) |
| 28..29 (u16LE) | предустановка болюса «Завтрак A 5:00–6:59» | × 0,025 Ед | `a1/11` [0..1 (u16LE)] | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 30..31 (u16LE) | предустановка болюса «Завтрак B 7:00–9:59» | × 0,025 Ед | `a1/11` [2..3 (u16LE)] | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 32..33 (u16LE) | предустановка болюса «Обед A 10:00–11:59» | × 0,025 Ед | `a1/11` [4..5 (u16LE)] | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 34..35 (u16LE) | предустановка болюса «Обед B 12:00–14:59» | × 0,025 Ед | `a1/11` [6..7 (u16LE)] | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 36..37 (u16LE) | предустановка болюса «Ужин A 15:00–17:59» | × 0,025 Ед | `a1/11` [8..9 (u16LE)] | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 38..39 (u16LE) | предустановка болюса «Ужин B 18:00–21:59» | × 0,025 Ед | `a1/11` [10..11 (u16LE)] | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 40..41 (u16LE) | предустановка болюса «Ночной A 22:00–23:59» | × 0,025 Ед | `a1/11` [12..13 (u16LE)] | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 42..43 (u16LE) | предустановка болюса «Ночной B 00:00–4:59» | × 0,025 Ед | `a1/11` [14..15 (u16LE)] | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 44..49 (6 байт) | метка времени статуса | `YY MM DD HH MM SS`, байт = значение (не BCD) | — | 🟢 — шаги 006, 007; №8 |
| 50 (u8) | язык помпы | `0`=Русский; `1`=English (порядок пунктов меню, №31) | `a1/32` [0, бит `0x40`] | 🟢 — шаги 013, 020; №16, №17, №30, №31, №37 |
| 51 (u8) | ВБС активна | `0`=нет; `1`=да | — | 🟢 — шаги 024; №38, №39; 1 после `a1/02`; 0 после `a1/05` (отмена) |
| 52..55 (u32LE) | остаток резервуара | × 0,001 Ед | — | 🟢 — шаги 007; №6, №8, №9, №38, №39 |
| 76..77 (u16LE) | базальная скорость по профилю | × 0,025 Ед/ч | — | 🟡 — шаги 007, 024; №8, №9, №38; 20 (0,5) до полуночи, 36 (0,9) после; при ВБС не меняется; с экраном расписания не сверено |
| 80..81 (u16LE) | ВБС — значение | смысл зависит от типа (байт[0]): `01` — Ед/ч с шагом 0,025; `00` — проценты. В `a3/00` off80 — итоговая скорость, 0,025 Ед/ч: при `01` равна значению, при `00` = ⌊off76 × % / 100⌋ | `a1/02` [2..3 (u16LE)] (по правилу, см. кодирование) | 🟢 — шаги 024; №38, №39; округление вниз проверено на одном значении (36 × 113 % = 40,68 → 40) |
| 82..83 (u16LE) | ВБС — тип | `0`=проценты; `1`=Ед/ч | `a1/02` [0] | 🟢 — шаги 024; №38, №39 |
| 84..85 (u16LE) | ВБС — длительность | шаги по 15 мин; в `a3/00` off84 — минуты (байт × 15) | `a1/02` [1] (статус = байт × 15) | 🟢 — шаги 024; №38, №39 |
| 86..87 (u16LE) | ВБС — прошло | число, мин | — | 🟡 — шаги 024; №38, №39; растёт с начала ВБС (0→1→4…22); экран показывает остаток, точная сверка не делалась |
<!-- /gen:a3_00 -->

**IOB** (акт. инсулин) в `a3/00` не обнаружен → предположительно вычисляется приложением (🟡).

<!-- gen:a3_00_unknown -->
**Неразобранные смещения** (29 из 88 байт): 10..12, 18..19, 22..23, 56..75, 78..79 — побайтово в `OBJECTS.md`.
<!-- /gen:a3_00_unknown -->

**off0** описан в таблице как гипотеза (🟡): индикатор батареи по ступеням напряжения; условие проверки — там же.

### 4.2. Поля объекта `a3/0c` (краткий статус, 20 байт данных)
<!-- gen:a3_0c -->
| Смещение | Поле | Кодирование | Команда | Статус |
|---|---|---|---|---|
| 2 (u8) | длительность сигнала тревоги | `0`=Длинный; `1`=Нормальный; `2`=Короткий (порядок пунктов меню, №27) | `a1/32` [7] | 🟢 — шаги 013, 018; №16, №17, №25, №26, №27 |
| 3 (u8) | напряжение батареи | × 0,01 В | — | 🟢 — шаги 003, 006; №5, №9 |
| 4 (u8) | звуковой режим | `0`=выкл; `1`=вкл | `a1/34` [0] | 🟢 — шаги 011, 016; №12, №13, №22, №23 |
| 5 (u8) | шаг звукового режима | `0`=0,1 Ед; `1`=0,5 Ед; `2`=1,0 Ед; `3`=5,0 Ед (порядок пунктов меню, №41) | `a1/34` [1] | 🟢 — шаги 011, 025; №12, №13, №41; значение — индекс пункта меню |
<!-- /gen:a3_0c -->

<!-- gen:a3_0c_unknown -->
**Неразобранные смещения** (16 из 20 байт): 0..1, 6..19 — побайтово в `OBJECTS.md`.
<!-- /gen:a3_0c_unknown -->

Процент заряда (46%) в кадре не обнаружен → предположительно вычисляется приложением из напряжения (🟡).

### 4.3. Единица дозирования инсулина
Инсулиновые величины помпы кодируются с шагом **0,025 Ед**. Подтверждено сменой значений: макс. болюс
(480→492 = 12,0→12,3), макс. базал (320→308 = 8,0→7,7), предустановки болюса (8 слотов, Шаг 025), ВБС
(240 = 6,0 Ед/ч, Шаг 024). Значения базальных профилей (`a3/08`) предположительно в тех же единицах
(`0x0014`=20 → 0,5; `0x0024`=36 → 0,9) — 🟡.

## 5. Управляющие команды

Класс `0xA1`; ответ помпы — квитанция `A1/55` (§2.2). Полезная нагрузка идёт после
идентификационного блока `<identity>` (§2.1).

<!-- gen:commands -->
| Код | Полезная нагрузка | Назначение | Статус |
|---|---|---|---|
| `0x02` | 4 байта: тип · длительность · значение u16LE (§5.4) | запуск временной базальной скорости (ВБС) | 🟢 — шаги 024; в окне: `01 03 f0 00` (6 Ед/ч, 45 мин), `00 0b 71 00` (113 %, 2:45) |
| `0x05` | нет | отмена ВБС | 🟡 — шаги 024; в окне одно наблюдение (04:01): поля ВБС в `a3/00` обнулились; до окна — 5 таких же случаев |
| `0x11` | 8 × u16LE, 0,025 Ед (§5.3) | предустановки болюса | 🟢 — шаги 012, 025 |
| `0x12` | `0c/12/14 00 00` | не установлено | 🟡 — шаги 011 |
| `0x31` | 6 байт — время `YY MM DD HH MM SS` | установка времени помпы | 🟢 — шаги 009 |
| `0x32` | 16 байт — блок настроек (§5.1) | запись настроек помпы | 🟢 — шаги 009, 011, 013 |
| `0x33` | `03 00` | не установлено (ответ — квитанция) | 🟡 |
| `0x34` | 16 байт — блок звука (§5.2) | звуковой режим и его шаг | 🟢 — шаги 011, 016; приложение отправляет `a1/34` вместе с `a1/32` при любой смене настроек |
<!-- /gen:commands -->

При каждом изменении настройки приложение отправляет **оба** блока — `a1/32` и `a1/34`. Значения блоков
отражаются в объектах статуса `a3/00` и `a3/0c` (§4.1, §4.2).

### 5.1. Блок настроек `a1/32` (16 байт payload после `<identity>`)
<!-- gen:a1_32 -->
| Байт(ы) | Поле | Кодирование | Статус (отражение) | Статус |
|---|---|---|---|---|
| 0, бит `0x01` | скорость болюса | `0`=Нормальная; `1`=Низкая | `a3/00` off2 | 🟢 — шаги 011, 014; №12, №13, №18, №19; меню целиком не снималось; наблюдались оба значения |
| 0, бит `0x02` | блокировка клавиатуры | `0`=выкл; `1`=вкл | `a3/00` off5 | 🟢 — шаги 013, 023; №16, №17, №31, №36, №37 |
| 0, бит `0x04` | автовыключение помпы | `0`=выкл; `1`=вкл | `a3/00` off6 | 🟢 — шаги 013, 022; №16, №17, №31, №34, №35, №37 |
| 0, бит `0x20` | лимит дневной дозы | `0`=выкл; `1`=вкл | `a3/00` off13 | 🟢 — шаги 013, 021; №16, №17, №25, №32, №33, №37 |
| 0, бит `0x40` | язык помпы | `0`=Русский; `1`=English (порядок пунктов меню, №31) | `a3/00` off50 | 🟢 — шаги 013, 020; №16, №17, №30, №31, №37 |
| 1 | вид сигнала тревоги | `0`=Звуковой; `1`=Вибрация; `2`=Звуковой и вибрация (порядок пунктов меню, №25) | `a3/00` off1 | 🟢 — шаги 013, 017; №16, №17, №24, №25 |
| 2 | яркость экрана помпы | `0`=10 %; `1`=30 %; `2`=50 %; `3`=60 %; `4`=80 %; `5`=100 % (порядок пунктов меню, №29) | `a3/00` off3 | 🟢 — шаги 013, 019; №16, №17, №28, №29; значение — индекс пункта меню |
| 3 | время автовыключения помпы | число, ч | `a3/00` off7 | 🟢 — шаги 013, 022; №16, №17, №34, №35; число часов, не индекс; в видимой части меню 2–8 ч (№35), 1 ч — на экране (№17); полный список не снят |
| 4 | «инсулин заканчивается» — единицы | число, Ед | `a3/00` off8 | 🟢 — шаги 013, 022; №16, №17, №25; число единиц; меню не снималось |
| 5 | «инсулин заканчивается» — время | число получасов (не индекс меню): `11`=05:30, `13`=06:30 | `a3/00` off9 | 🟢 — шаги 013, 022, 023; №16, №17, №25, №37; меню 02:00–12:00 шагом 0:30 (`4`…`24`) — со слов пользователя; на №37 видимы 02:00–05:00 |
| 6 | расширенный болюс / напоминание о ГК | битовое поле: bit0 = «Расширенный болюс» (разрешает растянутый и двойной болюс), bit1 = «Напоминание о ГК»; в `a3/00` off4 = байт × 2 (`00`/`02`/`04`/`06`) | `a3/00` off4 (статус = байт × 2) | 🟢 — шаги 011, 012, 015; №12, №13, №14, №20, №21 |
| 7 | длительность сигнала тревоги | `0`=Длинный; `1`=Нормальный; `2`=Короткий (порядок пунктов меню, №27) | `a3/0c` off2 | 🟢 — шаги 013, 018; №16, №17, №25, №26, №27 |
| 8..9 (u16LE) | автовыключение экрана | × 0,1 с | `a3/00` off14 | 🟢 — шаги 013; №16, №17, №31; меню не снималось; в эфире 150 и 600 |
| 10..11 (u16LE) | общая дневная доза (лимит) | число, Ед | `a3/00` off20 | 🟢 — шаги 013, 021, 022; №16, №17, №32, №33, №34; число единиц, не индекс; меню 50–300 шагом 50 (№33) |
| 12..13 (u16LE) | максимальный базал | × 0,025 Ед/ч | `a3/00` off24 | 🟢 — шаги 010; №11; 8,0→7,7 (320→308) |
| 14..15 (u16LE) | максимальный болюс | × 0,025 Ед | `a3/00` off26 | 🟢 — шаги 009; №10, №21; 12,0→12,3 (480→492) |
<!-- /gen:a1_32 -->

Биты `0x08`, `0x10`, `0x80` байта[0] в окне исследования не встречались (проверяется `tools/verify_fields.py`).

### 5.2. Блок настроек `a1/34` (16 байт)
<!-- gen:a1_34 -->
| Байт(ы) | Поле | Кодирование | Статус (отражение) | Статус |
|---|---|---|---|---|
| 0 | звуковой режим | `0`=выкл; `1`=вкл | `a3/0c` off4 | 🟢 — шаги 011, 016; №12, №13, №22, №23 |
| 1 | шаг звукового режима | `0`=0,1 Ед; `1`=0,5 Ед; `2`=1,0 Ед; `3`=5,0 Ед (порядок пунктов меню, №41) | `a3/0c` off5 | 🟢 — шаги 011, 025; №12, №13, №41; значение — индекс пункта меню |
<!-- /gen:a1_34 -->

### 5.3. Предустановки болюса — команда `a1/11`
Восемь значений u16LE (шаг **0,025 Ед**), по одному на слот; при редактировании приложение пересылает весь
список. Значения отражаются в `a3/00` off28..43.

<!-- gen:a1_11 -->
| Байт(ы) | Поле | Кодирование | Статус (отражение) | Статус |
|---|---|---|---|---|
| 0..1 (u16LE) | предустановка болюса «Завтрак A 5:00–6:59» | × 0,025 Ед | `a3/00` off28 | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 2..3 (u16LE) | предустановка болюса «Завтрак B 7:00–9:59» | × 0,025 Ед | `a3/00` off30 | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 4..5 (u16LE) | предустановка болюса «Обед A 10:00–11:59» | × 0,025 Ед | `a3/00` off32 | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 6..7 (u16LE) | предустановка болюса «Обед B 12:00–14:59» | × 0,025 Ед | `a3/00` off34 | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 8..9 (u16LE) | предустановка болюса «Ужин A 15:00–17:59» | × 0,025 Ед | `a3/00` off36 | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 10..11 (u16LE) | предустановка болюса «Ужин B 18:00–21:59» | × 0,025 Ед | `a3/00` off38 | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 12..13 (u16LE) | предустановка болюса «Ночной A 22:00–23:59» | × 0,025 Ед | `a3/00` off40 | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
| 14..15 (u16LE) | предустановка болюса «Ночной B 00:00–4:59» | × 0,025 Ед | `a3/00` off42 | 🟢 — шаги 012, 025; №14, №15, №40, №41 |
<!-- /gen:a1_11 -->

### 5.4. Временная базальная скорость — команды `a1/02`, `a1/05`
`a1/02` — запуск ВБС (4 байта), `a1/05` — отмена (без нагрузки). Состояние ВБС — в `a3/00` off51/80/82/84/86.

<!-- gen:a1_02 -->
| Байт(ы) | Поле | Кодирование | Статус (отражение) | Статус |
|---|---|---|---|---|
| 0 | ВБС — тип | `0`=проценты; `1`=Ед/ч | `a3/00` off82 | 🟢 — шаги 024; №38, №39 |
| 1 | ВБС — длительность | шаги по 15 мин; в `a3/00` off84 — минуты (байт × 15) | `a3/00` off84 (статус = байт × 15) | 🟢 — шаги 024; №38, №39 |
| 2..3 (u16LE) | ВБС — значение | смысл зависит от типа (байт[0]): `01` — Ед/ч с шагом 0,025; `00` — проценты. В `a3/00` off80 — итоговая скорость, 0,025 Ед/ч: при `01` равна значению, при `00` = ⌊off76 × % / 100⌋ | `a3/00` off80 (по правилу, см. кодирование) | 🟢 — шаги 024; №38, №39; округление вниз проверено на одном значении (36 × 113 % = 40,68 → 40) |
<!-- /gen:a1_02 -->

## 6. Поведение диалога и тайминги

_(правила диалога — см. RESEARCH.md, Этап F)_
